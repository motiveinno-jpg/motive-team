# [S3-독일제조사] [2차] 언어 전환 후 가입 탭 클릭 시 언어 초기화
- 심각도: P1
- 발견 시각: 2026-03-30 00:10
- 페이지: /app (제조사 로그인/가입)
- 재현 경로: /app → 🇩🇪 Deutsch 클릭 (독일어 적용) → Registrieren 탭 클릭 → 영어로 복귀 + URL ?lang=ja로 변경
- 기대 동작: Registrieren 클릭 후에도 독일어 유지
- 실제 동작:
  1. Deutsch 클릭 → 로그인 폼 독일어 정상 ("Anmelden", "Passwort")
  2. Registrieren(가입) 탭 클릭 → URL이 ?lang=ja로 변경
  3. 가입 폼이 영어로 표시 ("Your Company Inc.", "Full name")
  4. localStorage whistle_lang도 ja로 변경됨
- 언어 설정: de → ja (자동 변경)
- 스크린샷: /tmp/s3-r2-de-signup.png
- 상태: ✅ FIXED (2026-03-30)
- 수정 내용: switchAuthLang()에 URL ?lang= 파라미터 동기화 추가 — 언어 변경 시 stale ?lang=ja 잔존 방지. history.replaceState로 현재 URL 파라미터를 선택한 언어로 업데이트.
- 비고: authMode 전환 시 언어 파라미터가 유지되지 않음. switchAuthLang() 호출 후 authMode=signup 변경 시 이전 URL의 ?lang 파라미터 사용. 이전 세션의 ja 값이 잔존.
