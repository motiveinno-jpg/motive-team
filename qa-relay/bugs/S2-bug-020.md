# [S2-일본제조사] loadUser()가 URL ?lang=ja를 무시하고 독일어(de) 유지 — 근본 원인 확인
- 심각도: P0(서비스불가) — URL 파라미터로 언어 설정해도 로그인 시 덮어쓰여짐
- 발견 시각: 2026-03-29 23:55 (2차 루프)
- 페이지: /app?lang=ja → 로그인 후 전체 앱
- 재현 경로:
  1. localStorage에 whistle_lang=de (다른 세션 잔재)
  2. /app?lang=ja 접속 → 로그인 화면은 일본어 ✅
  3. 로그인 성공 → loadUser() 실행
  4. loadUser() line 5036: country=JP → _isKorean=false
  5. line 5041: _userLang!=='ko' → 조건 미해당 → _userLang=de 유지
  6. line 5043-5044: localStorage에 'de' 다시 저장
  7. 결과: 전체 앱이 독일어로 표시
- 기대 동작: ?lang=ja로 접속했으므로 일본어 유지
- 실제 동작: 독일어(de) UI — window.__whistleLang='de' 확인

## 근본 원인
loadUser() (line 5036-5047)의 국가→언어 매핑 로직이 부족:
1. country=KR일 때만 ko로 강제 (line 5040)
2. country=KR이 아닌 경우 && _userLang=ko이면 en으로 변경 (line 5041)
3. **그 외 조합은 기존 _userLang을 유지** → localStorage 오염 시 잘못된 언어 유지

## 수정 제안
```javascript
// loadUser() line 5036 이후 추가:
// 1. DB language 필드 최우선
if(r.data.language && supported.indexOf(r.data.language) >= 0) {
  _userLang = r.data.language;
}
// 2. URL param이 있으면 그것을 우선
else {
  var urlLang = new URLSearchParams(location.search).get('lang');
  if(urlLang && supported.indexOf(urlLang) >= 0) {
    _userLang = urlLang;
  }
}
// 3. country 기반 기본 매핑 (fallback)
else if(_authCountry) {
  var countryLangMap = {KR:'ko',JP:'ja',CN:'zh',DE:'de',VN:'vi',FR:'fr',TH:'th',ES:'es',PT:'pt',ID:'id',TR:'tr'};
  if(countryLangMap[_authCountry]) _userLang = countryLangMap[_authCountry];
}
```

- 스크린샷: /tmp/s2-r2-settings-ja.png, /tmp/s2-r2-subscription-ja.png
- 상태: ✅ FIXED (2026-03-30)
- 수정 내용:
  - loadUser() 언어 우선순위 재설계: URL ?lang= > DB users.language > country 매핑 > localStorage
  - 기존: localStorage 오염 시 잘못된 언어 유지됨
  - 수정: URL 파라미터 최우선, DB 언어 설정 2순위, country 매핑이 stale localStorage 덮어쓰기
