# [S2-일본제조사] 2차 — 제조사 대시보드가 독일어로 표시 (크로스 세션 언어 오염)
- 심각도: P1(핵심기능장애)
- 발견 시각: 2026-03-29 23:42
- 페이지: /app (제조사 대시보드)
- 재현 경로:
  1. qa.mfr.jp2@whistle-test.com (country=JP, language=ja)으로 세션 주입
  2. /app?lang=ja 접속 후 리로드
  3. 대시보드 전체가 독일어(de)로 표시
- 기대 동작: 일본어(ja) UI
- 실제 동작: 독일어(de) UI — "KI-Exportanalyse", "Dokumentenerstellung" 등
- 원인 추정:
  1. 동일 브라우저에서 S3(독일 제조사) QA가 localStorage에 de를 저장
  2. /app?lang=ja로 접속해도 whistle-app.htm의 loadUser()가 실행되면서 country=JP → _isKorean=false → _userLang이 localStorage의 de를 읽어서 독일어 유지
  3. loadUser() line 5036-5047에서 country=JP인데 _userLang='de'이면 변경하지 않음 (ko→en만 변경)
- 근본 원인: loadUser()가 비한국 국가에서 _userLang을 강제하지 않음. country=JP이면 _userLang=ja로 설정해야 하지만 현재 KR만 체크
- 제안:
  - loadUser()에서 country에 따른 기본 언어 매핑 추가:
    JP→ja, CN→zh, DE→de, VN→vi, FR→fr 등
  - 또는 DB의 language 필드를 우선시: if(r.data.language) _userLang=r.data.language
- 스크린샷: /tmp/s2-r2-buyer-dash-desktop.png
- 상태: OPEN
