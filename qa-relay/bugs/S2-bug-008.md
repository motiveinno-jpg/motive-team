# [S2-일본제조사] Accept-Language: ja-JP / navigator.language: ja 설정 시 자동 일본어 감지 실패
- 심각도: P0(서비스불가) — 글로벌 서비스에서 현지어 자동 감지 불가
- 발견 시각: 2026-03-29 22:25
- 페이지: whistle-ai.com (메인 랜딩), /app (로그인)
- 재현 경로:
  1. Accept-Language: ja-JP,ja;q=0.9 헤더 설정
  2. navigator.language = 'ja-JP' 오버라이드
  3. whistle-ai.com 접속 → 영어 랜딩 표시
  4. /app 접속 → 영어 로그인 표시
- 기대 동작: 일본어 UI 자동 표시 또는 최소한 일본어 선택 안내
- 실제 동작: 영어만 표시됨. 일본어 전환 방법을 알 수 없음.
- 언어 설정: ja (Accept-Language + navigator.language)
- 스크린샷: /tmp/s2-geo-landing.png, /tmp/s2-geo-app.png
- 근본 원인 분석:
  1. 랜딩 페이지(whistle-main.html)에는 _ML 딕셔너리 및 navigator.language 기반 자동 감지 없음
  2. whistle-app.html에는 navigator.language 감지 로직 있으나 (line 1350), 비인증 시 랜딩으로 리다이렉트되어 이 코드가 실행되지 않음
  3. 랜딩의 언어 옵션이 English/한국어만 제공 → 일본어 선택 불가
- 제안:
  1. 랜딩 페이지에 navigator.language 감지 추가 → ja 감지 시 자동 일본어 전환
  2. 또는 랜딩에 다국어 선택기 추가 (현재 English/한국어만)
  3. /app 비인증 접속 시 리다이렉트 대신 로그인 화면 표시 (현재 로직은 정상이나 비일관적)
- 상태: OPEN
- 연관: S2-bug-003 (언어 선택기 부재 — S6-fix-004로 수정 예정이나 미배포)
