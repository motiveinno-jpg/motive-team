# [S4-미국바이어] buyer_verifications 테이블 406 에러 | P2 | buyer-app.html

## 재현 경로
1. 바이어 로그인 성공
2. 대시보드 로드 중 네트워크 확인

## 기대
- 모든 API 요청 200 OK

## 실제
- GET /rest/v1/buyer_verifications?select=*&user_id=eq.{id} → 406
- 2회 발생 (대시보드 로드 시 2번 호출)
- 테이블 미존재 또는 스키마 불일치 가능성

## 콘솔 에러
- "Failed to load resource: the server responded with a status of 406 ()"

## ✅ FIXED (2026-03-30)
- 근본 원인: buyer_verifications 테이블에 코드에서 사용하는 컬럼 누락 (registration_number, country, submitted_at)
- migration 적용: add_missing_columns_to_buyer_verifications
- SELECT('*').maybeSingle()는 0행이면 null 반환 (정상), upsert 시 미존재 컬럼으로 406 발생
- RLS 정책은 정상 확인 (INSERT/UPDATE/SELECT on user_id = auth.uid())
