# S6-fix-008: 바이어 계정 role 교정 + log_role_change 트리거 수정

## 버그ID
- S2-bug-010 (P0): 로그인 후 /app/buyer → /app 리다이렉트
- S4-bug-002 (P1): 바이어 회원가입 후 제조사앱으로 리다이렉트
- S4-bug-005 (P1): 바이어 대시보드 리로드 시 제조사앱으로 리다이렉트

## 근본 원인
QA 테스트 계정 2개가 제조사 앱(/app)에서 먼저 생성됨 →
Supabase 트리거가 role='client'로 설정 →
바이어 포털에서 `users.role !== 'buyer'` 체크로 /app으로 리다이렉트

## 수정 내용
1. **DB 수정**: test-buyer-de@whistle-qa.com, test-buyer-jp@whistle-qa.com의 role을 'client'→'buyer'로 교정
2. **트리거 수정**: `log_role_change()` 함수에서 `OLD.role`/`NEW.role`(text)을
   `to_jsonb()`로 캐스팅 → `old_value`/`new_value`(jsonb) 컬럼과 타입 일치
   (이전: role 변경 시 타입 오류로 UPDATE 실패)

## 영향범위
- QA 테스트 계정의 바이어 포털 접근 정상화
- 향후 users.role 변경 시 audit_logs 정상 기록
